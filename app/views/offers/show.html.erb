<% content_for :title, "#{@offer.title}" %>
<% content_for :meta_description, "Page de vue d'une offre"%>

<%if @offer.type_offer.type_offer == "Service"%>
  <%= render 'offers/show_services' %>
<%else%>
  <%= render 'offers/show_products_adoptions' %>
<%end%>
