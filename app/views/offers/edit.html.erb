<% content_for :title, "Modifier une offre" %>
<% content_for :meta_description, "Page de modification d'une offre"%>

<section class="flex flex-col section mb-24 mt-24">
  <div class="flex flex-col w-full items-center mt-20">
    <div class="flex flex-col gap-6 bg-complementary rounded px-8 pt-6 pb-8 items-center">
      <h1 class="text-3xl">Modifier une annonce</h1>
      <%= render 'form', offer: @offer %>
    </div>
  </div>
</section>